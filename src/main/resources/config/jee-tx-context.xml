<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
                           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.0.xsd
    	                   http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd">

    <!-- JTA Container Configuration -->
    <bean id="datasource" class="bitronix.tm.resource.jdbc.PoolingDataSource" init-method="init" destroy-method="close">
        <property name="uniqueName" value="jdbc/jbpm-ds"/>
        <property name="className" value="bitronix.tm.resource.jdbc.lrc.LrcXADataSource"/>
        <property name="maxPoolSize" value="20"/>
        <property name="allowLocalTransactions" value="true"/>
        <property name="driverProperties">
            <props>
                <prop key="user">sa</prop>
                <prop key="password">sa</prop>
                <prop key="driverClassName">org.h2.Driver</prop>
                <prop key="url">jdbc:h2:mem:jbpm</prop>
            </props>
        </property>
    </bean>

    <bean id="btmConfig" factory-method="getConfiguration" class="bitronix.tm.TransactionManagerServices"/>

    <bean id="bitronixTransactionManager" factory-method="getTransactionManager" class="bitronix.tm.TransactionManagerServices" depends-on="btmConfig"
          destroy-method="shutdown"/>

    <context:annotation-config/>
    <tx:annotation-driven/>

<!--
    <aop:config>
        <aop:pointcut id="taskCommandExecutor" expression="execution(public * org.jbpm.services.task.commands.TaskCommandExecutorImpl.execute(..))"/>
        <aop:pointcut id="taskCommandService" expression="execution(public * org.jbpm.shared.services.impl.TransactionalCommandService.execute(..))"/>

        <aop:advisor advice-ref="transactionInterceptor" pointcut-ref="taskCommandExecutor"/>
        <aop:advisor advice-ref="transactionInterceptor" pointcut-ref="taskCommandService"/>
    </aop:config>

    <tx:advice id="transactionInterceptor">
        <tx:attributes>
            <tx:method name="*"/>
        </tx:attributes>
    </tx:advice>
-->

    <bean id="transactionManager" class="org.springframework.transaction.jta.JtaTransactionManager"
          depends-on="bitronixTransactionManager, datasource"/>
</beans>